import Form from '../Form';
import { StyledHeader } from './styles';

const Header = () => {
	return (
		<StyledHeader>
			<h1>Weather App</h1>
			<Form />
		</StyledHeader>
	);
};

export default Header;
